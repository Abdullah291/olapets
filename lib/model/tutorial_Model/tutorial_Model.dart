class TutorialModel {
  var image, title, subtitle, bgColor;
  bool? haveWidget;

  TutorialModel({
    this.image,
    this.title,
    this.subtitle,
    this.bgColor,
    this.haveWidget = false,
  });
}
